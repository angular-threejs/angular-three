// export function collide(a: THREE.Object3D, b: THREE.Object3D, tolerance: number) {
// 	const diffPos = a.position.clone().sub(b.position.clone());
// 	const d = diffPos.length();
// 	return d < tolerance;
// }
